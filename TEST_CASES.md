# OPIc 연습 앱 테스트케이스

## 목적

이 문서는 유스케이스를 구체적인 검증 시나리오로 바꾼다. 수동 테스트와 향후 자동화 테스트의 기준으로 사용한다.

## TC-01 학습 경로 표시

시나리오: 새 학습자가 앱을 연다.

단계:

1. 홈 화면을 연다.
2. 연습 목표 등급을 선택한다.
3. 학습용 서베이 주제를 선택한다.
4. 학습 경로로 이동한다.

기대 결과:

- 학습 경로 단계가 순서대로 보인다.
- 기록 기반 다음 추천 단계와 추천 이유가 표시된다.
- 같은 약점이 여러 기록에서 반복되면 반복 약점 기반 추천이 우선된다.
- 목표 등급은 공식 점수가 아니라 연습 목표로 표시된다.

## TC-02 오리엔테이션 문구

시나리오: 학습자가 오리엔테이션을 본다.

단계:

1. 오리엔테이션 단계를 연다.
2. 공식/비공식 설명을 확인한다.
3. 완료 표시한다.

기대 결과:

- 공식 ACTFL/OPIc 사실과 앱 근사치가 분리되어 있다.
- 제3자 자료는 학습 자료로 표시된다.
- 완료 상태가 새로고침 후에도 유지된다.

## TC-03 전략 관찰 보호 장치

시나리오: 학습자가 외부 학습 자료를 본다.

단계:

1. 전략 관찰 단계를 연다.
2. 원칙 노트를 추가하거나 체크한다.
3. 다음 단계로 진행한다.

기대 결과:

- UI는 베낀 스크립트가 아니라 원칙 노트를 유도한다.
- 제3자 콘텐츠를 앱 소유 콘텐츠처럼 내장하지 않는다.
- 단계를 완료할 수 있다.

## TC-04 진단 세트 흐름

시나리오: 학습자가 짧은 진단을 완료한다.

단계:

1. 진단 세트를 시작한다.
2. 각 프롬프트에 유효한 영어 답변을 제출한다.
3. 진단을 제출한다.

기대 결과:

- 진단은 전체 모의고사보다 짧다.
- 피드백은 Function, Accuracy, Content/Context, Text Type을 사용한다.
- 약점이 추천에 사용할 수 있도록 저장된다.

## TC-05 답변 재료

시나리오: 학습자가 주제 노트를 만든다.

단계:

1. 답변 재료 단계를 연다.
2. 주제를 선택한다.
3. 한국어 글머리표를 추가한다.
4. 저장한다.

기대 결과:

- 노트가 주제별로 저장된다.
- UI가 전체 영어 스크립트 작성을 지양한다.
- 저장한 노트가 새로고침 후에도 보인다.
- 첫 답변 재료를 저장하면 학습 경로의 답변 재료 정리 단계가 완료 처리된다.
- 최근 저장 시각이 표시된다.

자동화 범위:

- 오리엔테이션 미완료 시 오리엔테이션을 추천한다.
- 답변 재료가 없으면 답변 재료 정리를 추천한다.
- 역할극 약점이 있으면 역할극 집중 훈련을 추천한다.
- 최근 답변이 짧으면 핵심 말하기 훈련을 추천한다.
- 모의고사 결과가 없으면 전체 모의고사를 추천한다.
- 시간 사용 피드백이 있으면 약점 복습을 추천한다.

## TC-06 개별 연습 피드백

시나리오: 학습자가 하나의 프롬프트를 연습한다.

단계:

1. 개별 연습을 시작한다.
2. `질문 듣기`로 질문 음성을 재생한다.
3. 필요할 때 `질문 텍스트 보기`를 누른다.
4. 유효한 1차 답변을 제출한다.
5. 피드백을 확인한다.
6. 재답변을 제출한다.

기대 결과:

- 질문 텍스트는 기본적으로 숨겨져 있다.
- DS Interviewer 패널이 질문 음성 재생 진입점을 제공한다.
- 너무 짧은 답변은 API 호출 전에 차단된다.
- 피드백은 공식 정렬 기준을 포함한다.
- 샘플 답변은 1차 시도 후에만 보인다.
- 재답변 비교가 보인다.

## TC-07 Gemini 대체 응답

시나리오: Gemini 키가 없거나 API가 실패한다.

단계:

1. 유효하지 않은 Gemini 키로 실행하거나 API 실패를 시뮬레이션한다.
2. 유효한 답변을 제출한다.

기대 결과:

- 앱이 로컬 피드백으로 대체 응답한다.
- UI에 `Local fallback`이 표시된다.
- 앱은 계속 사용할 수 있다.

자동화 범위:

- feedback API route는 Gemini 실패 시 `provider: "local"`을 반환한다.
- compare API route는 Gemini 실패 시 local comparison을 반환한다.
- mock-report API route는 Gemini 실패 시 local report를 반환하고 Q1 warm-up을 제외한다.

## TC-08 모의고사 Q1 비채점

시나리오: 학습자가 전체 모의고사를 완료한다.

단계:

1. 모의고사를 시작한다.
2. Q1에 답한다.
3. 이후 문항에 답하거나 건너뛴다.
4. 리포트를 생성한다.

기대 결과:

- Q1은 warm-up/non-rated로 표시된다.
- 리포트는 Q1을 평가 연습 피드백에서 제외한다.
- 리포트에 Q1 제외 안내가 표시된다.

## TC-09 모의고사 건너뛰기와 중간 종료

시나리오: 학습자가 문항을 건너뛰고 중간 종료한다.

단계:

1. 모의고사를 시작한다.
2. 평가 연습 문항을 하나 이상 건너뛴다.
3. Q15 전에 시험을 종료한다.

기대 결과:

- 평가 연습 답변이 하나 이상 있으면 부분 리포트가 생성된다.
- Q1 warm-up만 있으면 리포트가 생성되지 않는다.
- 리포트에 종료 방식이 표시된다.

## TC-10 모의고사 타이밍 UX

시나리오: 학습자가 모의고사 중 시간 정보를 확인한다.

단계:

1. 모의고사를 시작한다.
2. 한 문항에서 권장 답변 시간보다 오래 머문다.
3. 남은 시간이 5분 이하인 상태를 확인한다.
4. 답변을 저장하고 다음 문항으로 이동한다.

기대 결과:

- 전체 남은 시간이 표시된다.
- 현재 문항 경과 시간이 표시되고 문항 이동 시 초기화된다.
- 권장 답변 시간이 표시된다.
- 권장 시간을 넘기면 마무리 안내가 표시된다.
- 전체 남은 시간이 5분 이하 또는 1분 이하일 때 경고가 표시된다.
- 저장된 모의고사 답변에는 문항별 소요 시간이 포함된다.
- 리포트에는 Q1 warm-up을 제외한 시간 사용 분석이 표시된다.

## TC-11 음성 중심 질문 표시

시나리오: 학습자가 실전감 있는 질문 표시 방식을 사용한다.

단계:

1. 개별 연습을 시작한다.
2. 모의고사를 시작한다.
3. 각 화면에서 질문 영역을 확인한다.
4. 질문 듣기와 질문 텍스트 보기를 사용한다.

기대 결과:

- 개별 연습과 모의고사 모두 DS Interviewer 패널을 사용한다.
- 질문 텍스트는 기본적으로 숨겨져 있다.
- 질문 듣기는 브라우저 음성 합성으로 질문을 재생한다.
- 모의고사 질문 듣기는 문항당 다시 듣기 1회로 제한된다.
- DS Interviewer는 공식 OPIc Ava 또는 공식 시험 화면으로 표현되지 않는다.

자동화 범위:

- 개별 연습의 질문 텍스트는 기본적으로 숨겨져 있고, `질문 텍스트 보기`를 누른 뒤 공개된다.
- 개별 연습의 `질문 듣기`는 브라우저 음성 합성을 호출한다.
- 모의고사의 `질문 듣기`는 1회 사용 후 남은 듣기 0회가 되고 다시 듣기 버튼이 비활성화된다.
- 모의고사에서도 `질문 텍스트 보기`를 누른 뒤 문항 텍스트가 공개된다.

## TC-12 공식 문구 점검

시나리오: 리뷰어가 앱 문구를 점검한다.

단계:

1. 홈, 설정, 연습, 모의고사, 리포트, 기록 화면을 확인한다.
2. 공식 점수처럼 보이는 표현을 검색한다.

기대 결과:

- 등급 추정은 advisory/practice reference로 표시된다.
- 내부 문제 유형은 공식 taxonomy처럼 표시되지 않는다.
- 학습용 서베이 주제는 정확한 공식 Background Survey 항목처럼 표시되지 않는다.

## TC-13 Background Survey 설정

시나리오: 학습자가 공개 ACTFL OPIc 예시에 가까운 Background Survey를 설정한다.

절차:

1. 설정 화면을 연다.
2. Part 1에서 종사 분야를 선택한다.
3. Part 2에서 학생 여부와 최근 수강 강의를 선택한다.
4. Part 3에서 거주 형태를 선택한다.
5. Part 4에서 여가, 취미, 운동, 휴가/출장 항목을 총 12개 이상 선택한다.
6. Self Assessment 문장을 선택한다.
7. 홈으로 돌아가 추천 질문과 모의고사 구성을 확인한다.

예상 결과:

- Background Survey가 4파트로 표시된다.
- Part 4 선택 개수가 표시되고 12개 미만이면 안내가 나온다.
- Self Assessment 선택은 앱의 연습 목표 등급에 반영된다.
- 선택한 주제는 추천 질문과 모의고사 주제에 반영된다.
- 화면은 공식 시험 UI 복제가 아니라 학습용 Background Survey임을 설명한다.
- 모의고사 패턴은 simulation pattern으로 표시된다.

## TC-14 로컬 저장

시나리오: 학습자가 새로고침 후 돌아온다.

단계:

1. 설정과 개별 연습 1회를 완료한다.
2. 브라우저를 새로고침한다.
3. 기록을 연다.

기대 결과:

- 설정이 유지된다.
- 연습 기록이 유지된다.
- 완료한 모의고사 결과도 유지된다.
- 저장한 답변 재료가 유지된다.

자동화 범위:

- 모의고사 시간 요약은 Q1 warm-up을 제외한다.
- 시간 기록이 있는 최근 3회 모의고사 기준 평균 답변 시간, 권장 시간 초과 문항 수, 짧은 답변 수를 계산한다.
- 시간 기록이 없는 과거 모의고사는 시간 추이 평균에서 제외한다.
- 반복 약점은 개별 연습 개선점과 모의고사 약점/복습/시간 피드백을 함께 집계한다.
- 기록 화면에는 반복 약점 카테고리와 반복 횟수가 표시된다.

## TC-15 빌드 검증

시나리오: 개발자가 변경사항을 검증한다.

단계:

1. `rtk npm test`를 실행한다.
2. `rtk npm run build`를 실행한다.

기대 결과:

- Vitest 단위 테스트가 성공한다.
- 빌드가 성공한다.
- TypeScript 검사가 통과한다.
- API route가 빌드 결과에 포함된다.
