# OPIc 연습 앱 제품 스펙

## 제품 목표

현실적인 말하기 연습, AI 코칭, 재답변 비교, 전체 모의고사를 통해 사용자가 OPIc 말하기 시험을 준비할 수 있는 반응형 웹앱을 만든다.

이 제품은 스크립트 암기 도구가 아니라 말하기 코치처럼 동작해야 한다.

## 대상 사용자

다음 OPIc 연습 목표 등급을 준비하는 사용자:

- IM1
- IM2
- IH
- AL

훈련 난이도, 권장 답변 길이, 피드백 깊이, 질문 선택은 사용자가 선택한 연습 목표 등급에 맞춰 달라져야 한다.

이 네 단계는 앱에서 지원하는 연습 목표 트랙이며, ACTFL/OPIc의 전체 공식 등급 체계를 완전히 대체하지 않는다. 공식 ACTFL 등급에는 Novice, Intermediate, Advanced, Superior 범위와 해당하는 Low/Mid/High 하위 단계가 포함된다. 앱의 레벨 선택은 공식 점수 선택이 아니라 학습 목표 선택으로 표시해야 한다.

## MVP 범위

MVP는 개인용 앱으로 시작하되 서비스 확장을 고려한 구조를 가진다.

포함:

- 반응형 웹앱
- 첫 버전에서는 로그인 없음
- 로컬 학습 기록 저장
- 연습 목표 등급 선택
- 학습용 Background Survey 선택
- 개별 연습 모드
- 재답변 비교
- 모의고사 모드
- AI 코칭 피드백
- 정적 질문은행
- 교체 가능한 AI/STT 구조

제외:

- 결제
- 관리자 대시보드
- 강사 계정
- 커뮤니티
- 네이티브 모바일 앱
- 정밀 발음 채점
- 클라우드 오디오 저장

## 핵심 모드

### 개별 연습

기본 흐름:

1. 추천 질문을 받거나 질문을 선택한다.
2. `DS Interviewer` 패널에서 질문을 음성으로 먼저 듣는다.
3. 질문 텍스트는 기본적으로 숨겨지고, 필요할 때만 버튼으로 공개한다.
4. 짧은 준비 시간을 가진다.
5. 1차 답변을 녹음하거나 입력한다.
6. 음성을 텍스트로 변환한다.
7. AI 코칭 피드백을 생성한다.
8. 1차 시도 이후에만 샘플 답변과 유용한 표현을 보여준다.
9. 재답변을 녹음하거나 입력한다.
10. 1차 답변과 재답변을 비교한다.

기본 동작은 학습자가 먼저 말하게 하고, 준비된 답변을 읽게 하지 않는 방향이어야 한다.

### 모의고사

모의고사는 단순화하되 현실적인 시험 흐름을 재현한다.

1. 학습용 Background Survey
2. 연습용 자기평가 난이도 선택
3. 짧은 오리엔테이션과 마이크 확인
4. 15개의 연속 OPIc 스타일 문항
5. 완료 후 전체 리포트

Background Survey는 종사 분야, 학생 여부와 수강 경험, 거주 형태, 여가/취미/운동/휴가 항목 선택의 4파트로 구성한다. Part 4는 실제 흐름에 맞춰 총 12개 이상 선택을 안내한다. Self Assessment는 6개 말하기 능력 진술 중 하나를 선택하게 하고 앱의 연습 목표 등급에 매핑한다.

모의고사 중에는 힌트나 샘플 답변을 보여주지 않는다. 피드백은 시험 종료 후에만 제공한다.

모의고사 문항은 `DS Interviewer`가 음성으로 들려주는 흐름을 기본으로 한다. 질문 텍스트는 실전감을 위해 숨겨두고, 학습 편의상 버튼을 눌렀을 때만 공개한다. 모의고사에서는 문제 다시 듣기를 문항당 1회로 제한한다.

MVP 모의고사는 40분 본시험 타이머를 사용한다. 이는 한국 OPIc 시험 경험에서 오리엔테이션과 본시험이 분리되고, 본시험을 40분 세션으로 보는 흐름에 맞춘 것이다.

모의고사 진행 중에는 전체 남은 시간, 현재 문항 경과 시간, 문항별 권장 답변 시간을 함께 보여준다. 전체 남은 시간이 5분 이하 또는 1분 이하가 되면 경고를 강화하고, 현재 문항이 권장 답변 시간을 넘으면 마무리 안내를 표시한다. 각 모의고사 답변에는 문항별 소요 시간을 저장한다.

모의고사 리포트에는 시간 사용 섹션을 포함한다. Q1 warm-up은 시간 분석에서도 제외하고, 평가 문항의 평균 소요 시간, 권장 답변 시간 초과 문항 수, 지나치게 짧은 답변 패턴을 학습 피드백으로 보여준다.

모의고사 구성은 공개 OPIc 구조를 참고한 앱 학습용 시뮬레이션이다. OPIc form은 Background Survey와 Self-Assessment 응답에 따라 개인화되므로, 앱은 고정 공식 form을 재현한다고 주장하면 안 된다.

공식 근거가 있는 모의고사 동작:

- OPIc에는 Introduction/Warm-Up 단계가 있다.
- ACTFL 공개 자료는 시험이 Ava가 응시자에게 자기소개를 요청하는 흐름으로 시작한다고 설명한다.
- ACTFL familiarization 자료는 warm-up activity가 rated가 아니라고 설명한다.

공식 고정 form으로 확인되지 않은 것:

- 모든 시험에 공통인 15문항 고정 배열
- 2-4번, 5-7번, 8-10번 같은 고정 주제 블록
- 11-13번 역할극, 14-15번 사회 이슈 고정 배치

앱은 이 구조를 한국 OPIc 대비용 학습 시뮬레이션 패턴으로 사용할 수 있지만, 공식 OPIc form이라고 표시하면 안 된다.

### 학습 경로

앱은 사용자가 개별 문제나 모의고사만 반복하지 않도록 가이드형 학습 경로를 제공해야 한다.

학습 경로 순서:

1. 오리엔테이션: OPIc가 어떤 시험인지, 공식/비공식 자료를 어떻게 구분하는지, 앱을 어떻게 사용할지 이해한다.
2. 전략 관찰: 제3자 전략 자료를 보며 코칭 원칙, filler(채움말) 사용, 비스크립트 답변 품질을 관찰한다.
3. 진단 세트: 짧은 문제 묶음에 답해 초기 약점을 파악한다.
4. 답변 재료: 선택한 연습 주제에 대해 한국어 글머리표로 답변 아이디어, 이야기, 예시, 이유를 정리한다.
5. 핵심 말하기 훈련: 묘사, 루틴, 과거 경험, 비교 답변을 연습한다.
6. 피드백 루프: 녹음/입력, 코칭, 표현 확인, 재답변, 비교를 반복한다.
7. 역할극 집중 훈련: 질문하기, 문제 설명, 대안 요청, 역할극 상황 대응을 집중 연습한다.
8. 미니 모의고사: 짧은 모의고사나 외부 모의고사 영상을 사용해 시험 흐름에 적응한다.
9. 전체 모의고사와 약점 복습: 40분 모의고사를 완료하고 리포트를 확인한 뒤 약점 연습으로 돌아간다.

외부 영상, PDF, 제3자 모의고사는 학습 자료다. 공식 OPIc 출처가 아니라 학습 참고 자료로 취급해야 한다. 앱은 사용자가 영어 스크립트를 암기하지 않고 원칙과 답변 아이디어를 추출하도록 유도해야 한다.

답변 재료 단계에서는 사용자가 선택한 학습 주제별로 한국어 메모를 저장한다. 각 주제는 이야기, 이유, 구체 예시 필드를 가지며, 최근 저장 시각을 표시한다. 첫 답변 재료가 저장되면 학습 경로의 답변 재료 정리 단계가 완료 처리된다.

학습 경로의 다음 추천 단계는 단순히 첫 미완료 단계만 사용하지 않는다. 앱은 답변 재료 저장 여부, 최근 개별 연습의 발화량과 개선점, 최근 모의고사 약점, 시간 사용 피드백, 모의고사 기록 유무를 함께 보고 다음 단계를 추천하며, 추천 이유를 한국어로 설명한다. 같은 약점이 여러 기록에서 반복되면 최근 1회 기록보다 반복 약점 패턴을 우선해 약점 복습이나 역할극 집중 훈련을 추천한다.

## 공식 정렬 정책

앱은 공식 OPIc/ACTFL 사실과 앱 학습용 근사치를 구분해야 한다.

공식 근거가 있는 내용:

- OPIc는 직장, 학교, 집, 활동, 관심사 등 Background Survey 정보를 사용한다.
- OPIc는 Self-Assessment를 사용해 적절한 test form 생성을 돕는다.
- OPIc는 개인화된 프롬프트가 있는 컴퓨터 기반 말하기 인터뷰다.
- OPIc는 암기되거나 리허설된 스크립트가 아니라 즉흥적 말하기 숙련도를 평가한다.
- OPIc 평가는 function, accuracy, content/context, text type을 종합적으로 고려한다.
- ACTFL Proficiency Guidelines는 FACT 기준, 즉 functions/tasks, accuracy, context/content, text type을 사용한다.

명확히 라벨링해야 하는 앱 학습용 근사치:

- `IM1`, `IM2`, `IH`, `AL` 네 개의 연습 목표 트랙
- `description`, `experience`, `comparison`, `roleplay`, `unexpected` 같은 내부 문제 분류
- 현재 학습용 Background Survey 주제 목록
- Background Survey라는 UI 문구는 공식 form 선택기가 아니라 앱 개인화를 위한 학습용 서베이라고 설명해야 한다.
- 학습용 서베이 주제는 공식 공개 넓은 범주와 좁은 앱 연습 주제를 구분해야 한다. 공식 공개 문구는 work, school, home, personal activities, interests 같은 넓은 범주를 뒷받침한다. movie, travel, food, technology, health 같은 좁은 항목은 별도 공식 출처가 없으면 앱 연습 주제다.
- Self-Assessment 또는 난이도 UI는 공식 form 생성이 아니라 앱 연습 난이도 설정이라고 설명해야 한다.
- 현재 15문항 모의고사 구성
- 2-4번 주제 블록, 11-13번 역할극, 14-15번 사회 이슈 같은 번호별 패턴
- Gemini 또는 로컬 대체 응답이 생성하는 예상 등급
- 공식 ACTFL/OPIc 문구와 직접 일치하지 않는 rubric 라벨

UI와 문서는 앱 학습용 근사치를 공식 ACTFL taxonomy, 공식 OPIc form 로직, 공식 점수 예측처럼 표현하면 안 된다.

## 피드백 전략

피드백은 코칭 중심이어야 한다. 예상 등급은 보조 정보이며 공식 점수가 아닌 연습용 참고 추정치로 표시해야 한다.

피드백은 가능한 범위에서 ACTFL/OPIc 공식 공개 평가 개념에 맞춘다. ACTFL OPIc 평가 요소와 Proficiency Guidelines가 강조하는 축:

- Function / 과제 수행
- Accuracy / 이해 가능성
- Content and Context / 내용과 맥락
- Text Type / 발화 유형과 조직화

피드백 언어:

- 한국어 중심 코칭
- 영어 유용 표현
- 영어 개선 예시

재답변 비교는 다음을 보여준다.

- 답변이 더 완성되었는지
- 구조가 개선되었는지
- 디테일이 늘었는지
- 반복 문제가 줄었는지
- 의미가 있을 경우 연습용 참고 등급 변화

기존 코칭 필드는 다음 기준에 매핑한다.

- `Task Fit` → Function, Content/Context
- `Structure` → Text Type
- `Detail` → Content/Context
- `Fluency` → Accuracy, Text Type
- `Language` → Accuracy
- `Level Match` → 공식 점수가 아니라 참고 정보

자세한 피드백 정책은 `ASSESSMENT_FEEDBACK_GUIDE.md`에서 관리한다.

## AI와 STT 전략

무료 우선, provider 교체 가능한 구조를 사용한다.

초기 권장:

- AI 피드백: Gemini 무료 티어 우선
- STT: 브라우저 Web Speech API 우선
- 대안: Gemini, Whisper, OpenAI transcription, 로컬 STT

제품 로직과 provider 계약을 분리한다.

- `AIProvider`
- `STTProvider`

현재 구현:

- 피드백과 재답변 비교는 서버 API route를 호출한다.
- 모의고사 리포트도 서버 API route를 호출한다.
- `GEMINI_API_KEY`가 설정되어 있으면 서버 route가 Gemini를 사용한다.
- Gemini가 없거나 실패하면 로컬 규칙 기반 코치로 대체 응답한다.
- API 키는 클라이언트 코드가 아니라 `.env.local`에 둔다.

## 오디오 저장 정책

MVP에서는 오디오 파일을 장기 저장하지 않는다.

- 현재 세션에서는 오디오 재생이 가능할 수 있다.
- 장기 기록은 transcript, metrics, feedback, comparison만 저장한다.
- 인증과 스토리지를 붙인 뒤 클라우드 오디오 저장 옵션을 추가할 수 있다.

## 질문은행

정적 JSON 또는 TypeScript 데이터 파일로 시작한다.

문항은 자체 제작 OPIc 스타일 연습 프롬프트여야 한다. 실제 시험 문항, 유출 문항, 유료 자료, 암기 스크립트를 복제하면 안 된다.

문항 설계는 공개된 공식 구조를 기준으로 한다.

- Background Survey 기반 개인화
- Self-Assessment 기반 난이도
- 개인화된 컴퓨터 기반 인터뷰 흐름
- 리허설이 아닌 숙련도 중심 말하기

앱의 문제 유형은 코칭과 추천을 위한 내부 학습 분류이며 공식 ACTFL taxonomy로 표시하지 않는다.

첫 질문은행 목표:

- 등급별 50문항
- 총 200문항

문항 필드:

- `id`
- `level`
- `type`
- `topic`
- `surveyTags`
- `sourceSurveyIds` 선택 필드: 세부 Background Survey 항목 기반 문항일 때 선택 항목 ID를 기록한다.
- `prompt`
- `followUpGroupId`
- `difficulty`
- `prepTimeSec`
- `answerTimeSec`
- `evaluationFocus`
- `sampleAnswer`
- `usefulExpressions`

문항 유형:

- 자기소개
- 묘사
- 루틴
- 경험
- 비교
- 문제 해결
- 역할극
- 돌발 주제

모의고사는 완전 랜덤이 아니라 주제 클러스터를 사용한다.

개별 연습 추천은 broad `surveyTags`뿐 아니라 `sourceSurveyIds`가 선택한 세부 Background Survey 항목과 맞는 문항을 우선 사용할 수 있다. 현재 질문은행은 공원, 캠핑, 요리, 걷기, 집에서 보내는 휴가, 콘서트, 주거 개선, 국내/해외 여행, 반려동물 항목의 첫 전용 문항 배치를 포함한다.

질문은행 확장 규칙은 `QUESTION_BANK_GUIDE.md`에서 관리한다.

## 학습 자료 정책

앱은 외부 학습 자료를 참조하거나 사용자가 추적하도록 할 수 있다. 단, 사용자가 개인 로컬 용도로 제공하고 권리가 허용되는 경우가 아니라면 저작권 있는 제3자 콘텐츠를 앱에 내장하면 안 된다.

학습 자료 예:

- OPIc 시험과 공부 접근법을 설명하는 오리엔테이션 영상
- 오픽노잼 같은 OPIc 전략 시리즈
- 개인 또는 제3자 PDF 노트
- YouTube 모의고사 영상

학습 자료는 다음을 지원한다.

- 전략 관찰
- 답변 재료
- 역할극 집중 훈련
- 미니 모의고사

학습 자료는 다음 용도로 쓰면 안 된다.

- 공식 OPIc 권위 주장
- 보호된 질문은행 복제
- 말하기 전에 영어 스크립트 암기 유도
- ACTFL/OPIc 공식 공개 기준 대체

## 로컬 데이터 모델

MVP 데이터는 로컬에 저장한다. 기록이 단순 설정을 넘어 커지면 IndexedDB가 적합하다.

저장할 데이터:

- 연습 목표 등급
- 학습용 서베이 선택
- 개별 연습 시도
- 모의고사 결과
- 답변 재료
- transcript
- feedback
- 재답변 비교
- 반복 약점
- 연습용 참고 등급 추이

나중에 Supabase Auth/Postgres로 옮기기 쉽도록 데이터 모델에는 선택적 `userId`를 둘 수 있다.

## 홈 화면

홈 화면은 코치형 대시보드여야 한다.

주요 초점:

- 오늘의 추천 연습
- 개선해야 할 약점
- 개별 연습 빠른 시작
- 모의고사 빠른 시작

보조 정보:

- 최근 연습 기록
- 연습용 참고 등급 추이
- 완료한 연습 수
- 최근 모의고사 시간 사용 추이

기록 화면의 시간 사용 추이는 Q1 warm-up을 제외한 평가 문항 기준으로 계산한다. 앱은 최근 모의고사의 평균 답변 시간, 권장 답변 시간 초과 문항 수, 매우 짧은 답변 수를 보여주고, 최근 3회 기준 요약을 제공한다.

기록 화면은 반복 약점도 요약한다. 개별 연습의 개선점, 모의고사 약점, 추천 복습, 시간 피드백을 묶어 발화량 부족, 구체 예시 부족, 역할극, 시간 초과, 짧은 답변, 구조 부족 같은 학습 카테고리로 집계한다.

## 구현 순서

1. 웹앱 shell 생성
2. 연습 목표 등급과 학습용 서베이 설정
3. 정적 질문은행과 추천 로직
4. 개별 연습 루프
5. STT provider 추상화
6. AI 피드백 provider 추상화
7. 재답변 비교
8. 로컬 기록 저장
9. 같은 연습 엔진을 활용한 모의고사 orchestration
10. 전체 모의고사 리포트

## 성공 기준

MVP는 사용자가 다음을 할 수 있을 때 성공이다.

- OPIc 연습 목표 등급을 선택한다.
- 학습용 Background Survey 주제를 선택한다.
- 개별 말하기 연습 세션을 완료한다.
- 한국어 코칭과 영어 표현 제안을 받는다.
- 같은 질문에 재답변하고 비교를 확인한다.
- 40분, 15문항 모의고사를 완료한다.
- 추천 복습이 포함된 전체 리포트를 확인한다.
