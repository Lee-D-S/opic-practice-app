# OPIc 연습 앱 구현 계획

## 전체 진행률

예상 MVP 진행률: **70%**

이 문서는 `PRODUCT_SPEC.md`를 기준으로 구현 진행 상황을 추적한다. 기능을 구현할 때마다 완료한 작업, 검증 결과, 다음 우선순위를 갱신한다.

## 현재 상태

구현 완료:

- Next.js + TypeScript 앱 골격
- 코치형 홈 화면
- IM1, IM2, IH, AL 연습 목표 등급 선택
- 학습용 서베이 주제 선택
- 정적 TypeScript 질문은행 구조
- 개별 연습 루프
- AI 호출 전 1차 답변 유효성 검사
- Gemini 피드백 API 라우트와 로컬 대체 응답
- 1차 답변 후 샘플 답변과 유용한 표현 공개
- 재답변 비교 API 라우트와 로컬 대체 응답
- 설정과 개별 연습 기록의 로컬 저장
- 설정, 난이도 선택, 오리엔테이션, 40분 타이머, 15문항, 최종 리포트를 포함한 모의고사 흐름
- Gemini와 로컬 대체 응답을 사용하는 모의고사 리포트 API 라우트
- 모의고사 결과 로컬 저장
- 자체 제작 OPIc 스타일 프롬프트를 위한 질문은행 정책 가이드
- ACTFL/OPIc 공개 기준에 맞춘 평가/피드백 가이드
- 개별 피드백에 Function, Accuracy, Content/Context, Text Type 기준 추가
- 등급 추정 UI를 공식 점수가 아닌 연습용 참고 정보로 표시
- 모의고사 Q1을 저장과 리포트 생성에서 warm-up/non-rated로 처리
- 모의고사 UI에서 15문항 배열을 공식 고정 form이 아닌 한국 OPIc 대비용 시뮬레이션 패턴으로 표시
- 모의고사에서 문항 건너뛰기, 중간 종료, 부분 리포트 생성 지원
- 학습용 서베이 주제를 공식 공개 넓은 범주와 좁은 앱 연습 주제로 구분
- IM2 8문항, IH 8문항의 첫 질문은행 확장 배치
- 학습 경로, 공식 정렬, 연습, 모의고사를 검증하는 유스케이스와 테스트케이스
- 공식 문구, 학습 자료, 학습 경로, 연습, 모의고사, 저장, 반응형 UI, 빌드 검증을 위한 수동 QA 체크리스트
- 학습 경로 MVP UI: 단계 목록, 진행률 저장, 다음 추천 단계, 진단/연습/모의고사/기록 연결, 답변 재료 placeholder

남은 gap:

- 질문은행은 샘플보다 확장되었지만 200문항 목표에는 아직 부족하다.
- 일부 구현 개념은 계속 공식 정렬 검토가 필요하다: 서베이 주제, 레벨 라벨, 자기평가 난이도, 내부 문제 유형, 모의고사 구성, 예상 등급 문구.
- 모의고사는 더 강한 질문 생성과 더 깊은 리포트가 필요하다.
- 모의고사 리포트 프롬프트와 UI도 개별 피드백만큼 공식 정렬 기준을 자세히 보여줄 필요가 있다.
- STT는 브라우저 Web Speech API를 UI에서 직접 사용하고 있으며 아직 정식 `STTProvider` 뒤에 있지 않다.
- 대시보드 추천은 단순하며 아직 기록 기반 추천이 강하지 않다.
- 모의고사 질문 생성은 구조는 있으나 template 기반이다.
- 학습 경로는 MVP UI와 로컬 진행률 저장이 구현되었지만, 답변 재료의 실제 메모 저장과 기록 기반 단계 추천은 아직 약하다.
- 모바일/데스크톱 전체 UI 검토가 아직 부족하다.
- 자동화 테스트가 아직 없다.

## 마일스톤

| 마일스톤 | 진행률 | 상태 | 완료 기준 |
| --- | ---: | --- | --- |
| 앱 shell | 100% | 완료 | 앱이 로컬에서 로드되고, 레이아웃/내비게이션이 동작하며, production build가 통과한다. |
| 온보딩/설정 | 80% | 진행 중 | 연습 목표 등급과 서베이가 로컬에 저장된다. reset/edit UX가 남아 있다. |
| 개별 연습 | 85% | 진행 중 | 1차 답변, 피드백, 샘플 공개, 재답변, 비교가 동작한다. |
| Gemini 피드백 | 85% | 진행 중 | Gemini 키가 있으면 Gemini를 사용하고 실패 시 로컬 대체 응답을 사용한다. 프롬프트는 공식 정렬 기준을 사용한다. 출력 보강이 더 필요하다. |
| 평가 기준 정렬 | 60% | 진행 중 | 공식 기준 가이드가 있고 개별 피드백은 FACT/OPIc 기준을 사용한다. 모의고사 리포트 UI/detail과 추가 문구 점검이 필요하다. |
| 재답변 비교 | 75% | 진행 중 | API route를 통한 비교가 동작한다. 더 풍부한 지표와 UI 개선이 필요하다. |
| 모의고사 | 85% | 진행 중 | 40분/15문항 흐름, Q1 warm-up/non-rated 처리, 시뮬레이션 패턴 표시, 건너뛰기/중간 종료, 부분 리포트, 최종 리포트가 있다. 타이밍 UX와 질문 세트 강화가 필요하다. |
| 질문은행 | 25% | 진행 중 | 데이터 모델, 작성 정책, 첫 IM2/IH 확장 배치가 있다. 등급별 50문항, 총 200문항까지 계속 확장해야 한다. |
| 공식 정렬 audit | 80% | 진행 중 | 정책 문서와 주요 UI 문구, 서베이 주제 라벨링이 정리되었다. 남은 모의고사/콘텐츠 확장 후 최종 검토가 필요하다. |
| STT 추상화 | 35% | 진행 중 | 브라우저 STT가 UI에서 동작한다. 정식 provider 계약과 대체 UX가 필요하다. |
| 로컬 기록/분석 | 45% | 진행 중 | 개별 연습과 모의고사 결과가 저장된다. 약점 집계와 추이 화면이 필요하다. |
| 코치 대시보드 | 45% | 진행 중 | 홈이 추천 연습과 기본 지표를 보여준다. 기록 기반 추천이 필요하다. |
| 학습 경로 | 55% | 진행 중 | 단계 목록, 진행률 모델, 진단 세트 진입, 답변 재료 placeholder, 기존 연습/모의고사/기록 연결이 동작한다. 답변 재료 저장과 기록 기반 추천이 필요하다. |
| 테스트/QA | 50% | 진행 중 | 빌드가 통과하고 유스케이스/테스트케이스/수동 QA 체크리스트가 있다. 자동화 테스트가 필요하다. |
| 문서 동기화 | 70% | 진행 중 | 제품 스펙, 구현 계획, 가이드 문서들이 있으며 기능 후 갱신 규칙이 있다. |

## 갱신 규칙

기능을 구현한 뒤에는 다음을 갱신한다.

1. 관련 마일스톤의 진행률과 상태
2. 새로 완료된 동작
3. 새로 발견한 gap
4. 다음 1-3개 우선순위
5. 실행한 검증 결과
6. 제품 동작이 바뀌었으면 `PRODUCT_SPEC.md`

진행률 기준:

- `0%`: 미착수
- `25%`: 타입, 구조, placeholder UI 존재
- `50%`: 주요 흐름 동작
- `75%`: 저장, API/대체 응답, 기본 오류 처리 포함
- `100%`: 빌드 통과, 수동 확인 가능, 문서 갱신 완료

## MVP 완료 기준

MVP는 사용자가 다음을 할 수 있을 때 완료로 본다.

- OPIc 연습 목표 등급 선택
- 학습용 서베이 주제 선택
- 추천 개별 연습 질문 시작
- 유효한 1차 답변 제출 후 Gemini 또는 로컬 대체 응답으로 한국어 중심 코칭 받기
- 1차 시도 후 유용 표현과 샘플 답변 보기
- 재답변 제출 후 비교 보기
- 힌트 없이 40분, 15문항 모의고사 완료
- 강점, 약점, 추천 연습, 연습용 참고 등급이 포함된 전체 모의고사 리포트 확인
- 같은 브라우저에서 나중에 돌아와 개별 연습 및 모의고사 기록 확인

## 검증 로그

- 2026-05-22: 모의고사 API와 UI 구현 후 `rtk npm run build` 통과
- 2026-05-22: 로컬 서버가 `http://127.0.0.1:3000`에서 HTTP `200` 응답
- 2026-05-22: `.env.local` 설정 후 Gemini 피드백 API가 `provider=gemini` 반환
- 2026-05-22: `QUESTION_BANK_GUIDE.md` 추가, 프롬프트는 복제 문항이 아니라 자체 제작 OPIc 스타일 연습 콘텐츠라고 명시
- 2026-05-22: `PRODUCT_SPEC.md`에 공식 정렬 정책 추가, 서베이 주제/내부 문제 유형/앱 목표 레벨/모의고사 구성/예상 등급을 근거가 없으면 학습용 근사치로 표시
- 2026-05-22: ACTFL OPIc 평가 요소와 Proficiency Guidelines FACT 기준에 기반해 `ASSESSMENT_FEEDBACK_GUIDE.md` 추가
- 2026-05-22: Q1 자기소개/warm-up 비채점은 공식 근거가 있으나 2-4/5-7/8-10/11-13/14-15 고정 배열은 공식 universal form으로 확인되지 않음을 문서화
- 2026-05-22: 개별 피드백을 Function, Accuracy, Content/Context, Text Type 기준으로 리팩터링하고 예상 등급 문구를 공식 점수가 아닌 연습용 참고 정보로 변경
- 2026-05-22: 피드백 기준 정렬 후 `rtk npm run build` 통과
- 2026-05-22: 모의고사 Q1 warm-up/non-rated 처리와 15문항 배열의 학습용 시뮬레이션 패턴 표시 구현
- 2026-05-22: 모의고사 warm-up/non-rated 변경 후 `rtk npm run build` 통과
- 2026-05-22: 연습 목표 등급, 학습용 서베이 주제, 연습용 자기평가 난이도, 내부 문제 유형이 공식 ACTFL/OPIc 구조처럼 보이지 않도록 UI 문구 갱신
- 2026-05-22: 공식 정렬 문구 audit 변경 후 `rtk npm run build` 통과
- 2026-05-22: 학습용 서베이 주제를 공식 공개 넓은 범주와 앱 연습 주제로 라벨링
- 2026-05-22: 학습용 서베이 라벨링 후 `rtk npm run build` 통과
- 2026-05-22: `QUESTION_BANK_GUIDE.md` 기준으로 IM2/IH 중심 자체 제작 OPIc 스타일 문항 16개 추가
- 2026-05-22: 첫 질문은행 확장 후 `rtk npm run build` 통과
- 2026-05-22: 모의고사 건너뛰기/중간 종료 control과 부분 리포트 보호 장치 추가
- 2026-05-22: 모의고사 건너뛰기/중간 종료 control 후 `rtk npm run build` 통과
- 2026-05-22: 학습 경로 도메인 개념과 제품 흐름 추가: 오리엔테이션, 전략 관찰, 진단 세트, 답변 재료, 핵심 말하기 훈련, 피드백 루프, 역할극 집중 훈련, 미니 모의고사, 전체 모의고사와 약점 복습
- 2026-05-22: 학습 경로 구현과 QA 기준을 고정하기 위해 `USE_CASES.md`, `TEST_CASES.md` 추가
- 2026-05-22: 공식 정렬, 학습 자료, 학습 경로, 연습, 모의고사, 저장, 반응형 UI, 검증 명령을 다루는 `QA_CHECKLIST.md` 추가
- 2026-05-23: 루트 Markdown 문서를 한국어로 정리
- 2026-05-23: 학습 경로 MVP UI와 localStorage 진행률 저장 구현. `rtk npm run build` 통과, `rtk npm run lint` 에러 없이 통과(기존 모의고사 타이머 effect dependency warning 1건 남음)

## 다음 작업

1. 모의고사 타이밍 UX 개선: 문항별 경과 시간과 더 명확한 시간 경고
2. 학습 경로 답변 재료 저장 구현: 주제별 한국어 메모, 최근 수정 시각, 경로 단계 완료 조건 연결
3. 저장, 피드백 대체 응답, 모의고사 리포트 동작에 대한 자동화 테스트 추가

## 우선순위 근거

- 앱은 공식 정렬 안전장치와 첫 질문은행 확장 배치를 이미 포함한다.
- 더 많은 문항을 추가하기 전에 학습자가 어떤 순서로 공부하는지 안내하는 학습 경로가 필요하다.
- 모의고사 타이밍 UX는 이후 모든 모의고사 문항에 영향을 준다.
